// 'use client';

// import { usePathname } from 'next/navigation';

// export default function geturi() {
//   const pathname = usePathname(); // Misalnya "/dashboard/products/123"
//   const segments = pathname.split('/').filter(Boolean); // ["dashboard", "products", "123"]
//   return segments;
// }
